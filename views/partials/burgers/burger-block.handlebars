<li>
	{{name}}
	{{#if devoured}}
	<button class="change-devoured  btn-outline-dark pull-right" data-id="{{id}}" data-newdevoured="{{devoured}}">
		Mark as Complete! 
	</button>
	{{/if}}

	{{!-- <button class="delete-burger" data-id="{{this.id}}">DELETE!</button> --}}
</li>
