{{!-- <div class="container">
  <div class="row">
    <div class="col-xs-12 text-center" id="top-bar">
      <h1>EAT DA BURGER!</h1>
    </div>
    <div class="col-xs-6 text-center" class="task">
      <div class="row">
        {{#each burgers}}
        {{#unless devoured}}
        <div class="col-xs-9 text-center">
          <pre>{{this.id}}. {{this.name}}</pre>
        </div>
        <div class="col-xs-3 text-center">
          <form method="POST" action="/burgers/update?_method=PUT" class="button-size">
            <input type="hidden" name="id" type="text" value={{this.id}}>
            <br>
            <button type="submit" class="btn btn-default">DEVOUR IT!</button>
          </form>
        </div>
        {{/unless}}
        {{/each}}
      </div>
    </div>
  </div>
</div> --}}

<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">CODING TO DO LIST</h1>
    <p class="lead">Welcome to my to do list! Add more to do's, mark them as complete!</p>
  </div>
</div>

<div class="container">
  <div class="row">



    {{!-- <div class="col-md-12">
    <h1 class="text-center">Burgers!</h1>
  </div> --}}

  <div class="col-md-6">

  
    <h2>To Do:</h2>

    <ul>
      {{#each burgers}}
      {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
      {{/unless}}
      {{/each}}
    </ul>
</div>
<div class="col-md-6">
    <h2>Done!</h2>

    <ul>
      {{#each burgers}}
      {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
      {{/if}}
      {{/each}}
    </ul>

</div>

<div class='col-md-6'>
    <h2>Add a To Do</h2>
    <form class="create-form">

      <div class="form-group">
        <label for="burg">Task Name:</label>
        <input type="text" id="burg" name="name">
      </div>

      {{!-- <div class="form-group">
        <label for="sleepy">Sleepy?</label><br>
        <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
        <input type="radio" name="sleepy" value="0"> Awake
      </div> --}}

      <button type="submit" class='btn-outline-dark'>Add To Do</button>
    </form>
    </col>
  </div>
</div>